import { FCWC } from 'types';

const RootLayout: FCWC = (props) => {
  const { children } = props;
  return (
    <html>
      <head></head>
      asds
      <body>{children}</body>
    </html>
  );
};

export default RootLayout;
